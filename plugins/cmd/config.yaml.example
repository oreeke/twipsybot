# Cmd 插件配置文件

enabled: false                    # 插件开关
priority: 999                     # 插件优先级（数字越大越先执行）

allowed_users:                    # 允许使用命令的用户列表
  - "admin@example.com"           # 用户 ID 或 username@host
  - "user1"
  - "user2"
